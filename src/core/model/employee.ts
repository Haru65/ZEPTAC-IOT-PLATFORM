import { getAssetPath } from "@/core/helpers/assets";

interface IEmployee {
  id: number;
  profile: string;
  name: string;
  mobile: string;
  role: string;
  company_name: string;
  h_id:number;
  a_id:number;
  p_id:number;
}

export type { IEmployee };
