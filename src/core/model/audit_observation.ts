// import { getAssetPath } from "@/core/helpeassets";

interface IAObservation {
    id: number;
    auditor_name: string;
    clause_name: string;
    clauses: string;
    company_id: string;
  }
  
export type { IAObservation };