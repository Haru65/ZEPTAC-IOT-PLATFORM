// import { getAssetPath } from "@/core/helpeassets";

interface IInstrument {
    id: number;
    model_no: string;
    name: string;
    description: string;
    quantity: string;
    created_at: string;
  }
  
  export type { IInstrument };
  